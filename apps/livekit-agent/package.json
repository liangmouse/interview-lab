{
  "name": "@interviewclaw/livekit-agent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm run kill && dotenv -e ../../.env.local -- tsx watch cli.ts dev",
    "kill": "pkill -9 -f 'tsx.*cli.ts dev' 2>/dev/null; pkill -9 -f 'job_proc_lazy_main.*main' 2>/dev/null; pkill -9 -f 'inference_proc_lazy_main' 2>/dev/null; sleep 1; echo 'All agent processes killed' || true",
    "download-files": "tsx cli.ts download-files",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@interviewclaw/ai-runtime": "workspace:*",
    "@interviewclaw/data-access": "workspace:*"
  }
}
